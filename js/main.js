const cartButton = document.querySelector("#cart-button");
comst modal = document.querySelector(.modal");
cartButton.addEventListener('click', function (event) {
    modal.classList.add("is-open");
});